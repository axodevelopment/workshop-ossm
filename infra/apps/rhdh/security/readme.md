
Config mapping ->

These will be secrets for rhdh dep to mapping 

# ── Redis ──────────────────────────────────────────────────────────────────

REDIS_PASSWORD: "123456-redis-password"

# ── PostgreSQL ─────────────────────────────────────────────────────────────
# map to postgres/deployment and rhdh
POSTGRES_USER: "backstage"
POSTGRES_PASSWORD: "123456-postgres-password"
POSTGRES_HOST: "postgresql"
POSTGRES_PORT: "5432"

# ── Keycloak OIDC ──────────────────────────────────────────────────────────
# must match the client secret value in realm-rhdh.yaml
KEYCLOAK_CLIENT_ID: "rhdh"
KEYCLOAK_CLIENT_SECRET: "wG03JcQaSP78in8riVrrJNq1Uyo8SbvT"

# ── Gitea ──────────────────────────────────────────────────────────────────
# generated by init/job-admin-init.yaml — grab from job logs after deploy
GITEA_TOKEN: "123456-gitea-rhdh-catalog-token"
GITEA_BASE_URL: "https://gitea.apps.snoaxolab.axodevelopment.dev"

# ── Branding ───────────────────────────────────────────────────────────────
# NOTE:
# generate with: base64 -i your-logo-full.svg
# Format must include the data URI prefix shown below
BASE64_EMBEDDED_FULL_LOGO: "data:image/svg+xml;base64,CHANGEME-base64-full-logo"
BASE64_EMBEDDED_ICON_LOGO: "data:image/svg+xml;base64,CHANGEME-base64-icon-logo"

# ── TLS (dev workaround for self-signed OCP router cert) ──────────────────
# need to get OCP ingress CA setup then replace this
NODE_TLS_REJECT_UNAUTHORIZED: "0"