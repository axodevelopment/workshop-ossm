apiVersion: v1
kind: Service
metadata:
  name: gitea-postgresql
  namespace: gitea
  annotations:
    argocd.argoproj.io/sync-wave: "-3"
spec:
  selector:
    app: gitea-postgresql
  ports:
    - port: 5432
      targetPort: 5432